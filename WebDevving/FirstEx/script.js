let nElem = document.getElementById('n')
let random = (Math.random() * 100)+1
let time = nElem.value 

nElem.addEventListener('change', ){

    setInterval(() => {

    }, 1000
    );
    
}

